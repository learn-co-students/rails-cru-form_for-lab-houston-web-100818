<h1>All Artists</h1>
<ul>
<% @artists.each do |artist| %>
  <li><%= link_to artist.name, artist %> <%= link_to 'Edit', edit_artist_path(artist) %> <%= link_to 'Delete', artist, method: :delete %></li>
<% end %>
<ul>
